name: CI
on: [ push, pull_request ]
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: Check links
      uses: actions/setup-node@v1
      run: node tests/checklinks.js docs/
